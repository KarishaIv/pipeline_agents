from typing import List

# YandexGPT настройки
YANDEX_GPT_CONFIG = {
    "folder_id": "указываем_сами",
    "iam_token": "указываем_сами",
    "model_uri": "gpt://указываем_сами/yandexgpt/latest",
    "temperature": 0.7,
    "rag_max_tokens": 1000,
    "final_max_tokens": 2000,
    "cache_dir": "cache",
}

# Категории новостей
CATEGORIES = ["Макроэкономика", "Банки", "Валюта", "Недвижимость"]

# Маппинг русских названий => английские (для Qdrant фильтра)
CATEGORY_SLUG = {
    "Макроэкономика": "macroeconomy",
    "Банки": "banks",
    "Валюта": "currency",
    "Недвижимость": "real_estate"
}

# Правила коммуникации
COMMUNICATION_RULES = {
    "max_rounds": 1,
    "allow_self_consultation": False,
}

# Промпт для оркестратора (планирование консультаций)
ORCHESTRATOR_PLANNING_PROMPT = """
Ты координатор мультиагентной системы. Есть 4 эксперта: {categories}.
Вопрос пользователя (для контекста): {question}
Целевая аудитория: {target_audience}
Текущие знания агентов о реакции ЦА на новости:
{agent_knowledge}
Задача: определи, каким агентам нужно проконсультироваться друг с другом (максимум 1 раунд), чтобы лучше понять реакцию ЦА на новости.
Агент не может обратиться к самому себе.
Верни список консультаций в формате JSON. Используй ТОЛЬКО эти ключи: from, to, query.
Пример ответа (не включай пояснений, только JSON):
[
{{"from": "banks", "to": "macroeconomy", "query": "вопрос..."}},
{{"from": "macroeconomy", "to": "currency", "query": "вопрос..."}}
]
Важно:
Используй английские названия категорий: macroeconomy, banks, currency, real_estate
Агент не может обратиться к самому себе (from != to)
Верни пустой список [], если консультации не нужны
Фокус на том, как новости влияют на восприятие ЦА, а не на решение
Ответ:
"""

# Промпт для финального вывода (КОНТЕКСТ РЕАКЦИИ, не решение)
FINAL_OUTPUT_PROMPT = """
Ты аналитик поведенческих реакций. На основе мнений 4 экспертов сформулируй контекст реакции целевой аудитории на новости.
Целевая аудитория: {target_audience}
Вопрос пользователя (для контекста): {question}
Мнения экспертов о реакции на новости:
{agent_opinions}
Консультации между агентами:
{consultations}

Задача: Опиши, как эта целевая аудитория может ВОСПРИНЯТЬ предоставленные новости.
НЕ отвечай на сам вопрос, дай только вывод. Твой вывод — это контекст для дальнейшей симуляции.

ВАЖНО:
1. Верни ответ СТРОГО в формате JSON. Без markdown, без пояснений.
2. Если целевая аудитория нереалистична (хоббиты, эльфы, коты, инопланетяне и т.д.):
   - В поле "summary_text" начни с: "ВНИМАНИЕ: АУДИТОРИЯ НЕРЕАЛИСТИЧНА. "
   - Добавь поле: "fictional_warning": true
   - Всё равно проанализируй запрос как гипотетический сценарий

Структура JSON:
{{
  "sentiment": "нейтральный / настороженный / заинтересованный / смешанный",
  "key_triggers": ["триггер 1", "триггер 2"],
  "barriers": ["барьер 1", "барьер 2"],
  "enablers": ["стимул 1", "стимул 2"],
  "summary_text": "Текст анализа (5-7 предложений)",
  "fictional_warning": false
}}
"""